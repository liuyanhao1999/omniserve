后台运行
nohup ./elegance-gateway > gateway.log 2>&1 &

nginx热重启
nginx -t
nginx -s reload

查看nginx配置
cd /www/server/panel/vhost/nginx
cat go_elegance_gateway.conf

看端口状态
netstat -antp | grep 8080

长命令
cd /www/wwwroot/elegance-gateway && nohup ./elegance-gateway > gateway.log 2>&1 &